import { Block } from "payload/types"

const Separator: Block = {
  slug: "separator",
  labels: {
    singular: "Content Separator",
    plural: "Content Separators",
  },
  fields: [],
}

export default Separator
